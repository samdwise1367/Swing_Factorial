
import javax.swing.SwingUtilities;

/*
A swing-based Java application that allows a user submit two integers, n & r and for each
submission, have a new row of results appended and displayed in a table (JTable) on the same
view as shown below:
 */

/**
 *
 * @author samdwise
 */
public class ExerciseMain {
    public static void main(String[] args) {
        ExerciseAction2 exerciseAction2 = new ExerciseAction2();
	            exerciseAction2.setGui();
                   
                }
	    }
